<h1>My Tasks</h1>
<br>
<br>
<div>
  <% @tasks.each do |task| %>
  <p><%=task.completed%> <%= link_to task.title, task_path(task) %></p>
  <% end %>
  <br>
  <br>
  <p><%= link_to "Add a new task", tasks_new_path %></p>
</div>
